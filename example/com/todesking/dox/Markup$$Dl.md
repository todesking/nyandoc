# com.todesking.dox.Markup$$Dl


```scala
case class Dl(items: Seq[DlItem]) extends Markup with Product with Serializable
```


 val items: Seq[DlItem]
------------------------

(defined at com.todesking.dox.Markup.Dl#items:Seq[DlItem])

